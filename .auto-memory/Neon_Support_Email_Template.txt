TO: support@neon.tech
SUBJECT: [URGENT] P0 — Authentication failure blocking all staging operations (Project fragrant-sea-98082526)

---

Hello Neon Support,

We are experiencing a critical authentication failure in our staging environment that is blocking all database operations. Despite successful password resets in the Neon Console, connections from Node.js and our NestJS API consistently fail with error code 28P01 (password authentication failed).

**Project Details:**
- Project ID: fragrant-sea-98082526
- Region: us-east-1
- Database: neondb
- User: neondb_owner (superuser)
- Timeline: Issue first observed 2026-05-09 17:00 UTC; persists through 2026-05-12 01:30 UTC (40+ hours)

**Reproduction:**

1. Using Node.js pg driver:
```bash
DATABASE_URL_UNPOOLED="postgresql://neondb_owner:npg_Tnv96ortDNpS@ep-shiny-moon-amaw1omz-c-5.us-east-1.aws.neon.tech/neondb?sslmode=require"
psql "$DATABASE_URL_UNPOOLED" -c "SELECT current_user;"
# Result: password authentication failed for user "neondb_owner" (SQLSTATE 28P01)
```

2. Using NestJS seed script:
```bash
pnpm --filter api seed:run -- --tenant=acme --type=personas
# Result: Error: password authentication failed for user 'neondb_owner' (code 28P01)
```

**Critical Finding:**
- ✅ Neon Console SQL Editor: Password works perfectly (can execute SELECT queries)
- ❌ libpq direct connection: Password fails with 28P01
- ❌ Node.js pg driver: Password fails with 28P01
- ❌ NestJS API: Cannot reach database (API health = 200 OK, but data operations fail)

This suggests the Neon Console may be using a different authentication path (websocket proxy?) that bypasses standard libpq password validation.

**Troubleshooting Completed:**
✅ Multiple password resets (5+ times)
✅ Tested with superuser role (neondb_owner)
✅ Removed TLS parameters (channel_binding=require) — no change
✅ Verified connection string format
✅ Confirmed password works in Console UI
❌ Cannot connect via Node.js, psql, or NestJS

**Requested Investigation:**

1. **Auth logs:** Backend authentication logs for failed libpq connection attempts from our staging environment
2. **Pooler config:** Current pgbouncer settings for this project
3. **Console comparison:** Why does Console SQL work but libpq fail? Do they use different auth paths?
4. **Network policy:** IP whitelist for this project — are Fly.io staging IPs (where our API is deployed) included?
5. **System state:** Any recent changes to our project? Platform issues affecting Node.js connections?
6. **Remediation:** Can you force restart the compute instance? Reset the role's auth state?

**Business Impact:**
- UAT testing deferred
- Cannot seed test data
- All staging API data operations blocked
- Timeline: URGENT — blocking feature delivery

**Additional Diagnostic Evidence** (Neon Console Testing 2026-05-12):

We conducted further diagnostics directly in Neon Console SQL Editor:

1. **Current User Test:**
   ```
   SELECT current_user, current_database();
   → Result: neondb_owner | neondb ✓ (309ms)
   ```
   Confirmed: Console session works perfectly with neondb_owner role.

2. **Role Configuration Test:**
   ```
   SELECT rolname, rolcanlogin, rolcreaterole, rolcreatedb, rolinherit
   FROM pg_roles WHERE rolname IN ('neondb_owner', 'sse_app', 'postgres');
   → Result: 2 rows
   - sse_app: rolcanlogin=true, role permissions correct
   - neondb_owner: rolcanlogin=true, is superuser (✓)
   ```
   Confirmed: Both roles properly configured for authentication.

3. **Infrastructure Status:**
   - Network: Public internet enabled ✓
   - Compute: 0.49/100 CU-hrs, idle ✓
   - Storage: 34.33 MB available ✓
   - All systems healthy

**Definitive Conclusion:**
The issue is NOT credentials, network, or infrastructure. The problem is specific to the authentication pathway:
- ✅ Neon Console (websocket proxy) → Connects successfully
- ❌ Node.js pg driver (direct TCP/libpq) → Fails with 28P01

This indicates pgbouncer or your auth layer is rejecting libpq connections from Node.js clients while accepting Console's websocket connections.

**Full Technical Context:**
See: .auto-memory/BUG-04_Neon_Support_Escalation.md
(Includes detailed error traces, diagnostics, hypotheses, and connection string analysis)

We are ready to provide additional logs or diagnostic information as needed. Please advise on next steps and the requested investigation details.

Thank you for your urgent attention to this critical issue.

Best regards,
Luigi Filippozzi
luigi.filippozzi@gmail.com
Storm Shield Enterprise (SSE)
